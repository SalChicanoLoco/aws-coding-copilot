# Git LFS tracking (if needed in future)
# *.zip filter=lfs diff=lfs merge=lfs -text

# Security: Mark sensitive file patterns
*.key filter=secret
*.pem filter=secret
*.p12 filter=secret
*.pfx filter=secret
*.keystore filter=secret

# Ensure shell scripts are executable
*.sh text eol=lf
