# Git-secrets patterns for AWS Coding Copilot
# Install git-secrets: https://github.com/awslabs/git-secrets

# Anthropic API Keys (96+ character keys starting with sk-ant-)
sk-ant-api03-[A-Za-z0-9\-_]{95,}

# AWS Access Keys
AKIA[0-9A-Z]{16}

# AWS Secret Keys (40 characters base64)
[A-Za-z0-9/+=]{40}

# Generic API key patterns
api[_-]?key['"\s]*[:=]['"\s]*[A-Za-z0-9\-_]{20,}

# Generic secret patterns
secret['"\s]*[:=]['"\s]*[A-Za-z0-9\-_]{20,}

# Private keys
-----BEGIN .*PRIVATE KEY-----

# Allowed patterns (these are safe examples/placeholders)
!sk-ant-\.\.\.
!YOUR_KEY_HERE
!YOUR_API_KEY
!REPLACE_WITH
!EXAMPLE_KEY
